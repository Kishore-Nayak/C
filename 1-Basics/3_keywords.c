/*
    C Language Keywords
    -------------------

    Data Types:
    ----------
    int        // Integer data type
    float      // Floating-point data type
    double     // Double-precision floating-point
    char       // Character data type
    void       // Empty data type (no value)

    Type Qualifiers:
    ----------------
    const      // Constant qualifier (read-only)
    volatile   // Volatile qualifier (prevents optimization on variables)
    signed     // Signed qualifier for integer types
    unsigned   // Unsigned qualifier for integer types

    Storage Class Specifiers:
    -------------------------
    auto       // Default storage class for local variables
    extern     // Specifies an external linkage for variables and functions
    static     // Keeps variable or function scope within the file
    register   // Requests storage in the CPU register (optional)

    Control Flow:
    ------------
    if         // Conditional statement
    else       // Specifies alternative for if statement
    switch     // Multi-way branch statement
    case       // Specifies a case in switch statement
    default    // Default case in switch statement
    break      // Exits from loop or switch
    continue   // Skips the current iteration of a loop

    Loops:
    ------
    for        // For loop
    while      // While loop
    do         // Do-while loop

    Functions:
    ----------
    return     // Returns a value from a function
    void       // Specifies a function returns no value

    Pointers and Memory Management:
    -------------------------------
    sizeof     // Returns the size of a data type or variable
    typedef    // Creates an alias for a data type
    struct     // Defines a structure
    union      // Defines a union
    enum       // Defines an enumeration

    Other:
    ------
    goto       // Jumps to a labeled statement
    const      // Specifies constant value
    volatile   // Prevents compiler optimizations
    inline     // Suggests inlining of a function

    Reserved for Special Uses:
    --------------------------
    _Alignas   // Specifies alignment of a variable or structure member (C11)
    _Alignof   // Yields the alignment requirement of a type (C11)
    _Atomic    // Atomic type qualifier (C11)
    _Bool      // Boolean data type (C99)
    _Complex   // Complex number data type (C99)
    _Generic   // Generic selection (C11)
    _Imaginary // Imaginary number data type (C99)
    _Noreturn  // Specifies a function that doesn’t return (C11)
    _Static_assert // Asserts a condition at compile-time (C11)
    _Thread_local // Thread-local storage (C11)

    Notes:
    ------
    - Some of these keywords are only available in C99 or C11 standards.
    - Keywords are case-sensitive and should not be used as variable names.
*/
